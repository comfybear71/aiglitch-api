## Summary
- Magic link create failed with **Invalid public key input** because `DEVNET_USDC_MINT` was a corrupted base58 string.
- Set Circle official devnet USDC mint `4zMMC9srt5Ri5X14GAgXhaHii3GnPAEERYPJgZJDncDU` (matches Circle faucet + Phantom).

## Changes
- `src/lib/trade/magic-claim/config.ts`: correct mint
- `docs/trade-magic-link-devnet.md`: document full mint

## Test plan
- [x] Devnet USDC mint validates as a Solana public key
- [ ] After deploy with devnet env: POST `/api/trade/magic-link/create` returns a transaction (not 502 Invalid public key)
- [ ] Trade preview Send shows USDC balance after matching trade-app mint PR
