## Summary
- Trade Send showed **0 USDC** on devnet while Phantom had 20 Circle USDC.
- `/api/trade/eligibility` uses `getWalletBalances()`, which always queried **mainnet** USDC mint.

## Changes
- `src/lib/solana-balance.ts`: use Circle devnet USDC mint when `NEXT_PUBLIC_SOLANA_NETWORK=devnet`.

## Test plan
- [ ] API deployed with devnet env: `GET /api/trade/eligibility?wallet=AEWv…` returns `balances.usdc` ~20
- [ ] trade.aiglitch.app Send → Magic Link shows USDC balance
