import { describe, it, expect } from "vitest";

describe("/api/movies", () => {
  it("returns director movies list", async () => {
    // DB-mock tests in follow-up
    expect(true).toBe(true);
  });
});
